# Enemy Trace Simulator

Enemy Trace Simulator is a standalone **Godot .NET / C#** diagnostic tool used to validate enemy movement behavior for the arcade game **Lady Bug**.

The repository is separate from the main Lady Bug remake project. Its job is not to become a second implementation of Lady Bug. Its job is to act as a microscope:

```text
MAME trace = reference
Godot / C# simulation = candidate
Enemy Trace Simulator = visual and diagnostic comparison harness
```

For detailed implementation notes, source-address mappings, reverse-engineering status, and workflow details, see:

```text
doc/current_implementation.md
```

## Current status

Current checkpoint: **v0.9.8**

Latest stable milestone:

```text
Multi-enemy-safe transition source-path inspector
```

v0.9.8 keeps the v0.9 visual replay validation workflow and makes the analytical source-path inspector explicit about its current scope:

```text
validated scope    = one active enemy, static player, reference-direction replay
out-of-scope now   = multi-enemy source-path analysis
future scope       = moving player, pivoting-door interaction, multi-enemy logic
```

The key v0.9.8 behavior is:

```text
active enemy count = 0 -> skip as no-active-enemy phase
active enemy count = 1 -> inspect the transition normally
active enemy count > 1 -> skip and report skippedMultiEnemyTransitions
```

This prevents the diagnostic tool from producing misleading source-path results when a second enemy becomes active.

## Current validated results

The current v0.9.8 inspector has been tested on a 1201-frame trace where a second enemy appears after the initial one-enemy phase.

Expected clean single-enemy result:

```text
visual replay comparison: comparedFrames=1201, mismatches=0
singleEnemyTransitions=827
skippedMultiEnemyTransitions=363
singleEnemyClean=true
multiEnemyMode=skip-until-explicitly-supported
```

Interpretation:

```text
- the side-by-side visual replay remains stable for the whole trace;
- the one-enemy source-path inspection remains clean;
- once a second enemy appears, the inspector stops interpreting those transitions;
- multi-enemy behavior is intentionally not validated yet.
```

Earlier v0.9.6 validation also confirmed:

```text
resultMismatchesSlot=0
resultMismatchesEnemyWork=0
sourceAcceptedButStaticBlockedProbes=0
missingVramInspections=0
```

This validated the transition-based source-path inspector and the mirrored static-maze direction mapping.

## What the tool currently does

### MAME trace generation

The tool can launch MAME using:

```text
config/mame_trace_settings.json
```

The normal JSONL trace is generated by:

```text
tools/mame/lua/ladybug_sequence_trace.lua
```

Recommended trace settings for current validation:

```json
{
  "luaScriptPath": "res://tools/mame/lua/ladybug_sequence_trace.lua",
  "outputPrefix": "ladybug_sequence_v8_fullmem",
  "framesAfterTick0": 600,
  "includeFullMemoryEachFrame": true,
  "includeLogicalMazeEachFrame": true,
  "enableDebugger": false
}
```

A longer trace is useful for detecting when the sequence leaves the one-enemy validation scope:

```json
{
  "luaScriptPath": "res://tools/mame/lua/ladybug_sequence_trace.lua",
  "outputPrefix": "ladybug_sequence_v8_fullmem_1200",
  "framesAfterTick0": 1200,
  "includeFullMemoryEachFrame": true,
  "includeLogicalMazeEachFrame": true,
  "enableDebugger": false
}
```

Do not enable the MAME debugger for the standard JSONL trace. Exact-PC diagnostics are a separate workflow.

### Visual replay

The main window displays two boards:

```text
left  = Simulation C# / Godot
right = Trace MAME
```

Playback controls:

```text
↺   restart from beginning
▶   resume playback
❚❚  pause playback
▶|  advance one tick
```

During playback, the simulator compares the currently displayed simulation state against the currently displayed MAME reference state.

If a mismatch is found:

```text
- playback pauses automatically;
- the display remains on the divergent tick;
- the status label reports the mismatch;
- the console prints the first mismatch details.
```

The visual comparator currently checks:

```text
player:  active, x, y, direction
enemies: active, x, y, direction
gates:   orientation and pivot
```

It intentionally ignores internal movement diagnostics such as:

```text
preferred[]
rejectedMask
fallback helper
chase timers
EnemyWork scratch values
```

Those fields are useful for analysis, but they should not stop the normal visual replay unless they affect visible behavior.

### Source-path decision inspector

The normal Compare action runs:

```text
Compare > Lady Bug reference-direction step
```

This uses a reference-direction replay to keep the visual comparison stable, then runs the source-path decision inspector as a diagnostic.

Current diagnostic:

```text
Lady Bug source-path decision inspector v0.9.8
```

It follows the arcade update timing:

```text
source input      = frame[i-1] enemy slot
reference result  = frame[i] enemy slot and EnemyWork
```

This matters because the displayed frame is already the committed result of the arcade update. Inspecting the displayed frame as if it were the routine input caused false positives in earlier diagnostics.

The inspector follows the source path only:

```text
0x427E decision gate
  carry set   -> preferred direction at 0x42E6
              -> current direction at 0x4325 if preferred is rejected
              -> fallback scan at 0x4241 if current is rejected
  carry clear -> outside-center keep / forced-reversal path at 0x433A
0x43BA one-pixel movement step
```

The old all-four-directions collision report is disabled in the normal path because it can ask questions the arcade source did not ask during that update.

### Den / lair release handling

Enemy release from the central den is special. The enemy starts inside the den and is forced through the exit even though the den is surrounded by walls on the left, bottom, and right.

The visual comparison treats inactive slots carefully:

```text
inactive vs inactive -> OK, position ignored
inactive vs active   -> mismatch
active vs inactive   -> mismatch
active vs active     -> compare x, y, direction
```

This avoids false mismatches for inactive enemy slots while still detecting a wrong release tick, wrong release position, or wrong den-exit direction.

## Important coordinate and direction conventions

### MAME-to-Godot vertical mirror

The simulator uses MAME RAM coordinates as the trace reference, but the Godot board displays them through the known vertical mirror:

```csharp
private const int MameYMirror = 0xDD;

private static int ToMameY(int godotArcadeY)
{
    return MameYMirror - godotArcadeY;
}
```

Equivalently:

```text
godotArcadeY = 0xDD - mameY
```

### Enemy direction encoding

Enemy movement uses this source encoding:

```text
01 = left
02 = up
04 = right
08 = down
```

Because of the MAME-to-Godot vertical mirror, source enemy directions must be mapped when comparing to `maze.json`:

```text
source 01/left  -> Godot maze left
source 02/up    -> Godot maze down
source 04/right -> Godot maze right
source 08/down  -> Godot maze up
```

This correction was validated in v0.9.6 and prevents false `sourceAcceptedButStaticBlocked` reports.

### Player direction / input encoding

Player direction/input uses a different vertical convention:

```text
01 = left
02 = down
04 = right
08 = up
```

Do not mix player and enemy direction encodings.

### Enemy decision center

Enemy decisions are aligned to:

```text
x & 0x0F == 0x08
y & 0x0F == 0x06
```

At each update the enemy moves by one arcade pixel. A new preferred/current/fallback decision is not taken at every pixel. Outside decision centers, the enemy normally keeps its direction, except for door/tile-related forced reversal cases.

## VRAM and maze conventions

### VRAM layout

Lady Bug background VRAM is column-major and bottom-to-top:

```text
D080-D09F = first column, bottom to top
D0A0-D0BF = second column, bottom to top
...
D3E0-D3FF = last column, bottom to top
```

Do not implement tile lookup as row-major screen order.

### `0x3C0A` tile lookup

Validated formula:

```text
HL = 0xD0A0 + ((D & 0xF8) * 4) + (E >> 3)
```

Meaning:

```text
D & F8  -> align probe X to 8-pixel tile column
* 4     -> convert to 32-byte column stride
E >> 3  -> tile index inside the column
D0A0    -> base used by this routine
```

### Logical maze and movement validator

The trace can include:

```text
6200..62AF = logical maze / navigation data
```

But the source-path movement validator uses:

```text
0x3911  logical maze validation through ROM table 0x0DA2
0x4130  local door / tile validation
```

Therefore, `0x6200..0x62AF` is useful diagnostic data, but it should not be treated as the complete final movement oracle.

## Historical notes

### v0.8 diagnostic bridge

v0.8.0 validated a replay bridge using exact-PC data such as `error.log`. This was useful but not final gameplay logic.

The v0.9 direction moved away from:

```text
MAME exact-PC logs -> runtime decisions -> perfect replay
```

toward:

```text
real Godot enemy logic -> candidate replay -> first divergence against MAME
```

### v0.9.6 correction

v0.9.6 corrected two important methodological issues:

```text
- inspect transitions frame[i-1] -> frame[i], not already committed frames;
- mirror vertical enemy directions when comparing source directions to maze.json.
```

### v0.9.8 correction

v0.9.8 made the source-path inspector multi-enemy safe by skipping unsupported multi-enemy transitions explicitly.

## Repository layout

```text
.
├─ assets/
├─ config/
├─ data/
├─ doc/
│  ├─ current_implementation.md
│  └─ diagnostics/
├─ scenes/
├─ scripts/
│  └─ tools/
│     ├─ trace/
│     ├─ comparison/
│     └─ simulation/
├─ tools/
│  └─ mame/
│     └─ lua/
└─ traces/
```

Important files:

```text
scenes/tools/EnemyTraceSimulator.tscn
scripts/tools/EnemyTraceSimulatorWindow.cs
scripts/tools/EnemyTraceBoardView.cs
scripts/tools/MameTraceLauncher.cs
scripts/tools/trace/MameTraceLoader.cs
scripts/tools/trace/EnemyTraceFrame.cs
scripts/tools/comparison/VisualReplayComparator.cs
scripts/tools/comparison/VisualReplayFrameConverter.cs
scripts/tools/comparison/VisualReplayMismatch.cs
scripts/tools/simulation/LadyBugEnemySimulationAdapter.cs
scripts/tools/simulation/LadyBugSourcePathDecisionInspector.cs
scripts/tools/simulation/LadyBugEnemyDecisionModel.cs
scripts/tools/simulation/LadyBugEnemyDecisionGate427EModel.cs
scripts/tools/simulation/LadyBugMameLocalTile4130Oracle.cs
scripts/tools/simulation/LadyBugGodotStaticMazeOracle.cs
tools/mame/lua/ladybug_sequence_trace.lua
doc/current_implementation.md
```

## Current limitations

- The left-side simulation is not yet a fully autonomous implementation of the real Lady Bug enemy AI.
- The normal comparison still uses a reference-direction replay adapter.
- Enemy release timing is still trace-derived.
- The source-path inspector is validated for one active enemy only.
- Multi-enemy transitions are skipped explicitly in v0.9.8.
- Moving-player traces are not implemented yet.
- Pivoting-door interaction as a gameplay input is not yet part of the validation workflow.
- Chase timers, round-robin behavior, full BFS/chase, and multi-enemy behavior are not independently validated yet.
- Exact-PC logs remain diagnostic aids only.

## Roadmap

Near-term priority:

```text
Validate one-enemy movement with a static player.
```

This includes:

```text
- keep the clean two-board visual replay as the main workflow;
- keep source-path transition inspection clean on several static-player traces;
- compare the real Godot one-enemy movement candidate against MAME;
- stop on the first visible mismatch;
- use the source-path inspector to explain the mismatch.
```

Later options, in a sensible order:

```text
1. player movement traces;
2. explicit pivoting-door interactions;
3. multi-enemy source-path analysis;
4. autonomous enemy release / slot activation;
5. chase activation and timers;
6. BFS / chase guidance;
7. regression trace fixtures.
```

Multi-enemy support is intentionally not the immediate priority. v0.9.8 only makes the current inspector safe when a trace leaves the one-enemy scope.

## Commit rhythm

Commit after meaningful milestones that:

```text
- compile;
- run in the Godot tool;
- produce a useful validation result or cleaner workflow;
- update README.md and doc/current_implementation.md when the strategy changes.
```

Avoid committing every tiny temporary diagnostic. Prefer stable, understandable checkpoints.
