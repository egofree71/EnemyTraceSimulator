# Current Implementation

**Project:** Enemy Trace Simulator  
**Current package version:** v0.9.14c  
**Latest validated milestone:** `Modeled visible BFS/chase preferred[] overrides for source-path single-enemy replay`  
**Engine target:** Godot Engine .NET 4.6.2  
**Language:** C#  

## Purpose of this document

This document describes the current implementation of Enemy Trace Simulator, the reverse-engineering assumptions it depends on, the validation workflows, and the next steps.

The root `README.md` gives the project overview. This file records the detailed implementation state.

## 1. Project goal

Enemy Trace Simulator is a standalone Godot/.NET diagnostic tool for validating and debugging enemy movement logic for a Lady Bug arcade remake.

The intended workflow is:

```text
1. run Lady Bug in MAME;
2. load a known MAME save-state;
3. export a deterministic JSONL reference trace;
4. load that trace in the simulator;
5. initialize a C# / Godot candidate simulation from the same state;
6. run the visual replay tick by tick;
7. compare the candidate against MAME;
8. stop at the first useful mismatch;
9. use deeper diagnostics to explain and fix the divergence.
```

The simulator should not become a second MAME-assisted game engine. Its role is to compare the real or candidate Godot enemy logic against the MAME reference.

## 2. v0.9 strategic shift

v0.8.0 validated a replay bridge. v0.9 changed the direction.

Previous diagnostic direction:

```text
MAME exact-PC logs -> replay provider -> runtime decisions -> perfect trace replay
```

Current v0.9 direction:

```text
MAME trace = reference
Godot / C# logic = candidate
Enemy Trace Simulator = visual comparison harness
```

This means:

```text
- standard JSONL traces are reference inputs;
- exact-PC logs are diagnostic aids;
- error.log must not become a normal gameplay dependency;
- the main success criterion is finding the first useful divergence;
- diagnostics should follow the source path, not ask artificial questions.
```

Checkpoint progression:

```text
v0.9.9b  first candidate replay computing active one-enemy movement through the source path
v0.9.10b default adapter/UI cleanup for source-path single-enemy replay
v0.9.11  preferred[] trace-sync preflight classifier
v0.9.12  hybrid preferred[] provider: model 0x2E97 rotate, keep random/BFS trace-synced
v0.9.13  random/R-low diagnostics: frame.r is not enough; exact-PC 0x2EA5 validates the model
v0.9.14b source-first BFS/chase guidance preflight using 0x45DC + 0x6200
v0.9.14c replay uses modeled visible 0x477D BFS/chase overrides when safe
```

## 3. Main UI workflow

The main window shows two boards:

```text
left  = Simulation C# / Godot
right = Trace MAME
```

The toolbar contains:

```text
⚙    edit MAME trace settings
MAME generate / launch trace capture
Load load the current JSONL trace
↺    restart visual sequence from the beginning
▶    resume playback
❚❚   pause playback
▶|   advance exactly one tick
Dump dump the current frame
Find helper navigation in the trace
Compare open analytical comparison tools
```

During playback, the simulator displays the current frame and compares the current visual state. If a mismatch occurs, playback pauses and the display remains on the divergent tick.

The visual comparator checks:

```text
player:  active, x, y, direction
enemies: active, x, y, direction
gates:   orientation, pivot
```

It intentionally does not compare internal diagnostic state such as `preferred[]`, rejected mask, fallback helper, chase timers, ports, timers, or raw memory blocks.

## 4. Board rendering

Implemented file:

```text
scripts/tools/EnemyTraceBoardView.cs
```

The board renders:

```text
- static logical maze from data/maze.json;
- rotating gates from the loaded trace;
- player sprite;
- enemy sprites;
- optional inactive enemy slots.
```

The old diagnostic overlay from the v0.7 single-enemy release focus has been removed from the normal display.

## 5. MAME trace workflow

### 5.1 Standard JSONL trace

Generated by:

```text
tools/mame/lua/ladybug_sequence_trace.lua
```

Recommended settings for one-enemy validation:

```json
{
  "luaScriptPath": "res://tools/mame/lua/ladybug_sequence_trace.lua",
  "outputPrefix": "ladybug_sequence_v914c_600",
  "framesAfterTick0": 600,
  "includeFullMemoryEachFrame": true,
  "includeLogicalMazeEachFrame": true,
  "enableDebugger": false
}
```

Longer traces can be used to verify that the replay detects the transition out of the one-enemy scope:

```json
{
  "luaScriptPath": "res://tools/mame/lua/ladybug_sequence_trace.lua",
  "outputPrefix": "ladybug_sequence_v914c_1200",
  "framesAfterTick0": 1200,
  "includeFullMemoryEachFrame": true,
  "includeLogicalMazeEachFrame": true,
  "enableDebugger": false
}
```

Do not enable the MAME debugger for the standard JSONL trace.

### 5.2 Exact-PC diagnostics

Generated by:

```text
tools/mame/lua/ladybug_enemywork_pc_trace.lua
tools/mame/lua/ladybug_preferred_pc_trace.lua
```

These produce diagnostic output such as `error.log` and analysis reports. They are not standard JSONL traces and should not be loaded through the normal trace loader.

## 6. Trace model

Important trace classes:

```text
scripts/tools/trace/EnemyTraceFrame.cs
scripts/tools/trace/EnemyTraceActor.cs
scripts/tools/trace/EnemyTraceGateState.cs
scripts/tools/trace/EnemyTraceEnemyWorkState.cs
scripts/tools/trace/EnemyTraceRawMemoryState.cs
scripts/tools/trace/MameTraceCoordinates.cs
```

A frame can include:

```text
schema, phase, frame/tick, mameFrame, pc, r, player, enemies, gates, enemyWork, timers, ports, rawMemory
```

Full-memory traces include:

```text
logicalMaze6200_62AF
ram6000_62AF
vramD000_D3FF
colorD400_D7FF
```

The raw-memory blocks are used by source-path, static maze, tile-level diagnostics, and BFS/chase guidance.

## 7. Coordinate conventions

### 7.1 MAME Y mirror

MAME RAM Y and Godot display Y are mirrored:

```csharp
private const int MameYMirror = 0xDD;

private static int ToMameY(int godotArcadeY)
{
    return MameYMirror - godotArcadeY;
}
```

Therefore:

```text
godotArcadeY = 0xDD - mameY
mameY        = 0xDD - godotArcadeY
```

### 7.2 Enemy decision center

Enemy movement decisions occur at enemy decision centers:

```text
x & 0x0F == 0x08
y & 0x0F == 0x06
```

### 7.3 Enemy direction encoding

Enemy source directions:

```text
01 = left
02 = up
04 = right
08 = down
```

When comparing source enemy directions to Godot `maze.json`, vertical directions must be mirrored:

```text
source 01/left  -> Godot maze left
source 02/up    -> Godot maze down
source 04/right -> Godot maze right
source 08/down  -> Godot maze up
```

### 7.4 Player direction / input encoding

Player direction/input uses a different vertical convention:

```text
01 = left
02 = down
04 = right
08 = up
```

Do not mix player and enemy direction encodings.

## 8. VRAM and maze conventions

### 8.1 VRAM layout

Lady Bug background VRAM is column-major and bottom-to-top:

```text
D080-D09F = first column, bottom to top
D0A0-D0BF = second column, bottom to top
...
D3E0-D3FF = last column, bottom to top
```

### 8.2 `0x3C0A` tile lookup

Validated formula:

```text
HL = 0xD0A0 + ((D & 0xF8) * 4) + (E >> 3)
```

### 8.3 Movement validator

The source-path movement validator uses:

```text
0x3911  logical maze validation through ROM table 0x0DA2
0x4130  local door / tile validation
```

### 8.4 BFS/chase guidance map

v0.9.14b/v0.9.14c validated the source-first BFS guidance lookup used before `0x477D`.

The source model is:

```text
0x45DC coordinate-to-logical-maze-index conversion:

column     = X >> 4
rowFromTop = 0x0A - ((Y - 0x30) >> 4)
index      = (rowFromTop << 4) | column
address    = 0x6200 + index
direction  = RAM[address] & 0x0F
```

In the source path, `0x4775/0x4776` reads the logical maze cell, and `0x477D` writes the low nibble into `preferred[slot]` if the value is valid.

Important timing detail:

```text
source input     = frame[i-1]
reference result = frame[i]
```

For BFS/chase guidance, the previous/source frame position gives the clean prediction.

## 9. Source-first enemy movement references

Primary reverse-engineering reference:

```text
LadyBug_enemy_management_extract.txt
```

Important source concepts:

```text
0x05AE  enemy slot allocation / release helper
0x3061  initialize released enemy slot
0x2E5C  preferred[] base generation routine
0x2E97  deterministic rotate preferred[] branch
0x2EA5  exact LD A,R value after random load, before AND 0x0F
0x2EC7  random/R-register preferred[] write branch
0x3911  logical maze validation
0x3C0A  tile address lookup
0x4130  local door / tile validation
0x4189  forced reversal tile probe
0x4241  fallback scan
0x427E  decision gate
0x42BA  Enemy_UpdateOne-style movement body
0x43BA  apply one-pixel movement
0x45DC  enemy pixel coordinate to logical maze index
0x46D8  chase / BFS override entry
0x477D  BFS preferred[] overwrite
```

Core enemy movement principles:

```text
- fixed tick;
- integer arcade-pixel positions;
- one-pixel movement steps;
- direction decisions at enemy centers only;
- preferred direction is generated before enemy slot updates;
- preferred direction is validated before use;
- fallback order is left, up, right, down;
- door-local validation can reject a direction;
- outside center, an enemy normally continues straight;
- door-related forced reversal can happen outside center;
- chase/BFS can overwrite preferred direction but does not bypass validation.
```

## 10. Compare adapters

Implemented / relevant files:

```text
scripts/tools/simulation/LadyBugEnemySimulationAdapter.cs
scripts/tools/simulation/LadyBugSourcePathSingleEnemyReplayAdapter.cs
scripts/tools/simulation/LadyBugPreferredHybridProvider.cs
scripts/tools/simulation/LadyBugPreferredTraceClassifier.cs
scripts/tools/simulation/LadyBugPreferredRandomRLowProbe.cs
scripts/tools/simulation/LadyBugPreferredBfsChasePreflight.cs
scripts/tools/simulation/LadyBugBfsChaseSourceModel.cs
scripts/tools/simulation/LadyBugMonsterPreferenceSystem.cs
scripts/tools/simulation/LadyBugSourcePathDecisionInspector.cs
scripts/tools/simulation/LadyBugEnemyDecisionModel.cs
scripts/tools/simulation/LadyBugEnemyDecisionGate427EModel.cs
scripts/tools/simulation/LadyBugMameLocalTile4130Oracle.cs
scripts/tools/simulation/LadyBugGodotStaticMazeOracle.cs
```

### 10.1 Default adapter wrapper

Current default adapter:

```text
LadyBugEnemySimulationAdapter
```

It is intentionally a small stable wrapper around:

```text
LadyBugSourcePathSingleEnemyReplayAdapter
```

### 10.2 Source-path single-enemy replay

Compare mode:

```text
Lady Bug source-path single-enemy replay
```

For one-active-enemy transitions, this adapter computes the visible enemy movement step through the reconstructed source path rather than using the reference direction:

```text
0x427E decision gate
  carry set:
    preferred 0x42E6
    current   0x4325 if preferred rejected
    fallback  0x4241 if current rejected
  carry clear:
    outside-center keep / forced reversal 0x433A
then:
  0x43BA one-pixel movement step
```

Still synchronized from the trace in v0.9.14c:

```text
release timing
random 0x2EC7 preferred[] base tuples
player
gates
timers
VRAM context
inactive slots
multi-enemy frames after scope limit
```

Modeled in v0.9.14c:

```text
one-enemy source-path movement
0x2E97 rotate preferred[] tuples
safe/visible 0x477D BFS/chase preferred[] overrides
```

### 10.3 Hybrid preferred[] provider

Implemented file:

```text
scripts/tools/simulation/LadyBugPreferredHybridProvider.cs
```

Mode:

```text
preferredMode=hybrid-modeled-2E97-rotate-and-visible-477D-bfs-else-trace-synced
```

Behavior:

```text
- compute the deterministic 0x2E97 rotate tuple from the player current direction;
- if the modeled rotate tuple matches the observed trace tuple, use the modeled tuple;
- if a safe BFS/chase override is detected and source-guidance matches, apply the modeled override;
- otherwise, fall back to the trace preferred[] tuple;
- keep reporting how often each path was used.
```

This means v0.9.14c removes trace-sync for two safe preferred[] families:

```text
0x2E97 rotate base tuple
visible 0x477D BFS/chase override
```

It intentionally keeps random `0x2EC7` trace-synced.

### 10.4 Preferred[] trace preflight

Implemented file:

```text
scripts/tools/simulation/LadyBugPreferredTraceClassifier.cs
```

It classifies observed preferred[] tuples into source families:

```text
2E97_ROTATE_FROM_..
2EC7_RANDOM_RLOW_..
477D_SLOT0_BFS_OVER_...
```

Expected clean output:

```text
tupleChecks > 0
classifiedMatches = tupleChecks
classifiedMismatches=0
unclassified=0
missingPreferred=0
clean=true
```

### 10.5 Random/R-low probe and exact-PC diagnostics

Implemented files:

```text
scripts/tools/simulation/LadyBugPreferredRandomRLowProbe.cs
tools/mame/lua/ladybug_preferred_pc_trace.lua
scripts/tools/simulation/LadyBugPreferredPcLogAnalyzer.cs
```

v0.9.13 showed that the standard JSONL `frame.r` value is not sufficient to reproduce `0x2EC7` autonomously.

The exact-PC diagnostic validated:

```text
A@0x2EA5 low nibble -> direction written at 0x2EC7
```

The key diagnostic result was:

```text
paired 2EA5->2EC7 events: 1676
exact direction matches from A@2EA5 low nibble: 1676/1676
first exact random mismatch: none
```

This validates the model, but it is not used in the normal replay because reproducing exact Z80 `R` timing is not necessary for the current goal.

### 10.6 BFS/chase preflight and source model

Implemented files:

```text
scripts/tools/simulation/LadyBugPreferredBfsChasePreflight.cs
scripts/tools/simulation/LadyBugBfsChaseSourceModel.cs
```

v0.9.14b changed the BFS/chase preflight from a naive maze probe to a source-first model based on `0x45DC`.

v0.9.14c uses this source model in the replay when safe.

Expected clean output:

```text
visibleBfsOverrideFrames > 0
chaseTimerActiveVisibleBfs = visibleBfsOverrideFrames
chaseTimerInactiveVisibleBfs=0
notBaseNotBfsVisible=0
sourceGuidanceBestMatches=visibleBfsOverrideFrames/visibleBfsOverrideFrames
sourceGuidanceClean=true
clean=true
```

Example validated source-guidance case:

```text
enemy=(5D,76)
idx=65
addr=6265
cell=(5,6)
byte=72
low=02
```

## 11. Source-path decision inspector

The inspector remains useful for explaining transitions and validating the source path.

It uses:

```text
source input     = frame[i-1] enemy slot
reference result = frame[i] enemy slot and EnemyWork
```

The all-four-directions collision report is disabled in the normal Compare output because it can create false positives by testing directions the source never tried.

## 12. Multi-enemy-safe mode

v0.9.8 and later do not implement multi-enemy source-path analysis. They prevent misleading reports.

Transition classification:

```text
activeStart=0 -> no-active sync
activeStart=1 and activeResult=1 -> inspect or replay normally
activeStart>1 or activeResult>1 -> skipped / synced multi-enemy transition
```

For the 1201-frame test trace under v0.9.14c:

```text
modeledSingleEnemyTransitions=827
multiEnemySyncTransitions=358
modeledClean=true
fullScopeClean=false
firstMultiEnemySync startTick=842 resultTick=843 activeStart=1 activeResult=2
```

## 13. Current validation status

Validated on multiple 600-tick static-player traces:

```text
visual replay comparison: mismatches=0
modeledSlotMismatches=0
modeledEnemyWorkMismatches=0
modeledClean=true
fullScopeClean=true
firstModeledProblem: none
```

Representative v0.9.14c 600-tick result:

```text
modeledSingleEnemyTransitions=595
preferredRejectedCurrentKept=1
fallbackSelected=8
modeledSlotMatches=595
modeledEnemyWorkMatches=595
preferredModeledRotateTransitions=204
preferredModeledBfsTransitions=117
preferredTraceFallbackTransitions=274
```

Second 600-tick result:

```text
modeledSingleEnemyTransitions=590
preferredAccepted=25
fallbackSelected=7
modeledSlotMatches=590
modeledEnemyWorkMatches=590
preferredModeledRotateTransitions=204
preferredModeledBfsTransitions=101
preferredTraceFallbackTransitions=285
```

Validated on a 1200-tick trace that reaches two active enemies:

```text
visual replay comparison: mismatches=0
modeledSingleEnemyTransitions=827
multiEnemySyncTransitions=358
modeledSlotMismatches=0
modeledEnemyWorkMismatches=0
modeledClean=true
fullScopeClean=false
preferredModeledRotateTransitions=283
preferredModeledBfsTransitions=186
preferredTraceFallbackTransitions=358
firstMultiEnemySync startTick=842 resultTick=843 activeStart=1 activeResult=2
```

BFS/chase guidance validation:

```text
visibleBfsOverrideFrames=136
sourceGuidanceBestMatches=136/136
sourceGuidanceClean=true
clean=true
```

This proves the current one-enemy replay remains clean, deterministic rotate preferred[] tuples can be modeled safely, visible BFS/chase overrides can be modeled from source guidance, and unsupported multi-enemy transitions are still reported instead of being mis-modeled.

## 14. Current limitations

Current known limitations:

```text
- release timing is still trace-derived;
- random 0x2EC7 preferred[] base tuples remain trace-synced by design;
- only safe/visible BFS/chase overrides are modeled in the replay;
- player state is still trace-synced;
- gates, timers, VRAM context, inactive slots, and multi-enemy frames are still trace-synced;
- den-exit logic is not autonomous;
- true multi-enemy replay is not implemented;
- moving-player traces are not implemented yet;
- pivoting-door interaction as a gameplay input is not yet part of the validation workflow;
- chase timer scheduling and round-robin selection are still trace-observed in the current replay;
- exact-PC logs remain diagnostic aids only.
```

The random limitation is intentional for now. The goal is to validate enemy movement with a static player, not to reproduce the exact Z80 refresh-register sequence. The Godot game can later use a normal C# random source to produce similar preferred-direction variation.

## 15. Next milestones

### Priority: validate one-enemy movement with static player

This priority is largely satisfied for the current static-player traces:

```text
- source-path movement is modeled;
- deterministic rotate preferred[] is modeled;
- visible BFS/chase guidance overrides are modeled;
- random remains trace-synced intentionally;
- visual and EnemyWork comparisons remain clean in the tested mono-enemy scope.
```

Possible next choices:

```text
1. run v0.9.14c on a few more static-player traces if desired;
2. decide whether the validated mono-enemy/static-player movement is good enough to port/refactor into the main Godot game;
3. consider release timing / den-exit only if it blocks the next validation goal;
4. add traces where the player moves;
5. add explicit pivoting-door interaction traces;
6. add true multi-enemy source-path analysis later.
```

Multi-enemy support is intentionally not the next priority. v0.9.8 and later only make the current code safe when a trace leaves the one-enemy scope.

## 16. Documentation rhythm

Update documentation at significant milestones:

```text
- strategy change;
- workflow change;
- new stable validation mode;
- new trace format requirement;
- important reverse-engineering correction;
- removal of obsolete diagnostic paths;
- validation scope changes such as multi-enemy-safe skipping;
- replay scope changes such as v0.9.9b source-path single-enemy replay;
- adapter/UI cleanup such as v0.9.10b default source-path Compare action;
- real reduction of trace-sync dependency such as v0.9.12 hybrid preferred[] provider;
- source-modeled BFS/chase preferred[] replay such as v0.9.14c.
```

Do not update the documentation for every tiny temporary counter or throwaway experiment.

Recommended commit practice:

```text
1. implement milestone;
2. verify compile/run;
3. update README.md and doc/current_implementation.md;
4. commit the code and documentation together.
```
